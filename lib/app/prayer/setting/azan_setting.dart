import 'package:flutter/material.dart';

class AzanSetting extends StatelessWidget {
  const AzanSetting({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}